

int	*ft_range(int min, int max)
{
	int il
	int *s;

	s = (int *)malloc(sizeof(int) * (max - min + 1));
	while (int <= max)
	{
		s[i] = min;
		min++;
	}
	if (s == NULL)
		return (NULL);
	return (s);
}
